import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.studieplusplusgui',
  appName: 'studieplusplusgui',
  webDir: 'dist'
};

export default config;
